# tunnel-gen-wireguard
Generates basic interface files for WireGuard VPN for both server and peers. Accepts command line arguments for generic creation, along with JSON for more of a customized generation. 
